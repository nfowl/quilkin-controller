apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quilkin-controller.fullname" . }}-test-connection"
  labels:
    {{- include "quilkin-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quilkin-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
